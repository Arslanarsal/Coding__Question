#include <bits/stdc++.h>
using namespace std;

class Solution
{

public:
    int removeStones(vector<vector<int>> &stones)
    {
        int maxrow = *max_element(stones[0].begin(), stones[0].end());
        int maxcol = *max_element(stones[1].begin(), stones[1].end());
    }
};

int main()
{

    return 0;
}