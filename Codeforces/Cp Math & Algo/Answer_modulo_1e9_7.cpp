#include <bits/stdc++.h>
using namespace std;

/*
( a + b) % M = ( ( a % M ) + ( b % M ) ) % M
( a * b) % M = ( ( a % M ) * ( b % M ) ) % M
( a – b) % M = ( ( a % M ) – ( b % M ) + M ) % M
( a / b ) % M = ( ( a % M ) / ( b ^-1 ) % M  ) % M
*/
int main()
{

    return 0;
}